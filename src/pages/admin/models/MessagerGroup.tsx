import React from 'react';
import PrismaTable from 'components/PrismaTable';

const MessagerGroup: React.FC = () => {
  return <PrismaTable model="MessagerGroup" />;
};

export default MessagerGroup;
